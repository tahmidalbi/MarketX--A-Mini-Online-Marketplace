spring:
  application:
    name: MarketX

  docker:
    compose:
      enabled: false

  datasource:
    url: jdbc:postgresql://localhost:5432/marketx
    username: postgres
    password: 2021

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081